# Staging Crypto Notes

**Format Signature**: hex (unifié sign/verify)  
**Canonicalisation**: fonction partagée `_canonicalizeApprovalPayload()`  
**Keypair Verification**: signature/vérification test message  
**Fingerprints**: SHA-256(SPKI DER) dans KeyRegistry
